/* A program to compute the factorial value of an input */
/* The errors contained in this file are expression 
   statement errors. One is a missing operater and
   the other is an unknown var type.
*/

void main(void) {
  int x; int fac; /* hello */
  double y;
  x = input();
  fac = 1;

  while (x > 1) {
    fac = fac * x;
    x = x  1;
  } 

  output(fac);
  
}